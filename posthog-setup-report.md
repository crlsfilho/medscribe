<wizard-report>
# PostHog post-wizard report

The wizard has completed a deep integration of PostHog analytics into Steto (medscribe). The following changes were made:

**Infrastructure:**
- Installed `posthog-js` (client-side) and `posthog-node` (server-side) packages
- Created `instrumentation-client.ts` — initializes PostHog on every page load via the Next.js 15.3+ instrumentation file, with session replay, error tracking, and a reverse proxy
- Created `src/lib/posthog-server.ts` — singleton PostHog Node client for server-side API routes
- Updated `next.config.ts` — added `/ingest/*` reverse proxy rewrites to reduce ad-blocker interference
- Added `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN` and `NEXT_PUBLIC_POSTHOG_HOST` to `.env.local`

**User identification:** Users are identified on both the client (login, register pages) and server (register API route) using their email as the distinct ID, ensuring client and server events are correlated.

**Event tracking added across 8 files:**

| Event | Description | File |
|---|---|---|
| `user_registered` | Fired when a new user successfully creates an account | `src/app/(auth)/register/page.tsx` |
| `user_registered_server` | Server-side confirmation of account creation, with `identify()` call | `src/app/api/auth/register/route.ts` |
| `user_logged_in` | Fired on successful login, with `identify()` call | `src/app/(auth)/login/page.tsx` |
| `login_failed` | Fired when login attempt fails with invalid credentials | `src/app/(auth)/login/page.tsx` |
| `onboarding_completed` | Fired when doctor completes profile onboarding flow | `src/components/onboarding-flow.tsx` |
| `visit_started` | Fired when a new clinical visit is created (patient data submitted) | `src/app/(dashboard)/consulta/nova/page.tsx` |
| `recording_completed` | Fired when audio recording for a visit ends | `src/app/(dashboard)/consulta/nova/page.tsx` |
| `soap_generated` | Server-side: AI SOAP note generated successfully | `src/app/api/generate-soap/route.ts` |
| `visit_saved` | Fired when doctor manually saves an edited SOAP note | `src/app/(dashboard)/consulta/[id]/page.tsx` |
| `soap_exported_pdf` | Fired when doctor exports SOAP note as PDF | `src/app/(dashboard)/consulta/[id]/page.tsx` |
| `document_generated` | Server-side: clinical document (prescription, certificate, exam) generated by AI | `src/app/api/generate-document/route.ts` |
| `document_signed` | Server-side: document digitally signed via VIDaaS | `src/app/api/signature/sign-document/route.ts` |

## Next steps

We've built some insights and a dashboard for you to keep an eye on user behavior, based on the events we just instrumented:

- [Analytics basics dashboard](/dashboard/1565069)
- [New User Registrations](/insights/dln5vTLO) — daily new doctor sign-ups
- [Active Doctors (Weekly)](/insights/ae7Ae1UG) — weekly unique doctors starting visits
- [SOAP Notes Generated](/insights/6C1M7X4s) — daily AI-generated SOAP notes
- [Registration to SOAP Funnel](/insights/51AKaaGl) — conversion from registration → visit → SOAP generation
- [Document & Signature Actions](/insights/TbG3b9X0) — documents generated vs digitally signed

### Agent skill

We've left an agent skill folder in your project at `.claude/skills/integration-nextjs-app-router/`. You can use this context for further agent development when using Claude Code. This will help ensure the model provides the most up-to-date approaches for integrating PostHog.

</wizard-report>
